import { h, render } from 'preact';
import AppRouter from './router'; // Adjusted import path

// Render the AppRouter component which includes the App component
render(<AppRouter />, document.getElementById('root'));